namespace Xilium.Crdtp.Client
{
    // TODO(dmitry.azaraev): (VeryLow) CrdtpEncoding type should not be part of client.
    public enum CrdtpEncoding
    {
        Json = 0,
        Cbor = 1,
    }
}
